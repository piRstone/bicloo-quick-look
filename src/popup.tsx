import { MemoryRouter } from 'react-router-dom'

import Routing from '~routes'

import "./style.css"

const IndexPopup = () => {
  return (
    <MemoryRouter>
      <Routing />
    </MemoryRouter>
  )
}

export default IndexPopup
